<!--
Template: project layer, CLAUDE.local.md.
This layer always exists, for every project, but this specific file is
optional: it only shows up once there is something that belongs here.
Gitignore this file. Delete this comment block once filled in.

What this file holds: anything that cannot be committed to this repo but
still matters to how work gets done in it, because a third party reads
this repo (credentials, account names, an internal position toward a
client, a partner's personal data, anything sensitive in a repo someone
else will read).
What it never holds: anything that could instead be committed. If it can
be committed, it belongs in AGENTS.md, not here.
-->

# CLAUDE.local.md (<project name>)

Not committed. See `.gitignore`.

## What lives here and why

<!-- one line per item: what it is and why it cannot go in AGENTS.md -->
