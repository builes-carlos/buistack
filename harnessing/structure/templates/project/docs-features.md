<!--
Template: project layer, docs/features/<area>.md.
One file per feature area, created the first time that area gets built out
past a trivial size. Delete this comment block once filled in, and name
the file after the area it documents (docs/features/billing.md, not
docs/features/feature.md).

What this file holds: the detail that would otherwise bloat CONTEXT.md past
readability. CONTEXT.md stays the map; this is the detail for one region of
it.
What it never holds: method, or a gotcha that applies outside this one
feature area.
-->

# <feature area name>

## What this covers

<!-- one or two sentences: the boundary of this area -->

## How it works

<!-- the detail: data flow, key files, the decisions that are not obvious
     from reading the code cold -->

## Updated by

Whoever builds or changes this area updates this file in the same commit.
It is not a log, it describes current behavior only, so an out-of-date
paragraph gets corrected in place, not appended to.
