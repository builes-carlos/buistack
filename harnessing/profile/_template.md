---
language: en
register: neutral
---

# Harnessing Profile: <name>

> Copy this file into your harnessing instance (created by `harnessing-init`),
> rename it if you like, and fill in the sections below. This is what
> parameterizes the doctrine for one person, without touching the framework
> itself. The framework stays generic and public; this file is what makes it
> yours, and it belongs in your own instance folder, never in `buistack` itself.
>
> Where that folder lives and how you keep it is up to you: version it, sync it,
> or leave it as a plain folder. It holds your own configuration and nobody else
> needs to read it, so the only thing that matters is that it is not public.

## Language and register

<!-- what language conversation should happen in, and any register detail
     that matters (formal or informal address, regional wording to use or
     avoid) -->

## Hard constraints

<!--
Things an agent must never do without asking, specific to how this person
works. Examples of the shape, not defaults to copy: never run a destructive
command without confirmation, never touch a production database directly,
never message a third party on this person's behalf.
-->

## How I work

<!--
Anything that changes how an agent should operate day to day: whether this
person runs or verifies things by hand, whether they talk to stakeholders
directly or need drafts prepared for them, what tools or budget constraints
shape default choices (e.g. free-tier tools only).
-->

## Notes

<!-- anything else worth an agent knowing before the first session -->
